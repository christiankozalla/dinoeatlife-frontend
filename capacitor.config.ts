import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'dinoeatlife-frontend',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
